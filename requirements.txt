# cxr-mcp — Horizon container dependencies
# GPU inference runs on Modal; this container does image preprocessing only.
# No torch, no transformers, no model weights needed here.

fastmcp[tasks]>=3.2.4
pillow>=10.0
requests>=2.31
numpy>=1.24
pydantic>=2.0
